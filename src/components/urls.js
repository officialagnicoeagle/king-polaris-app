// API endpoint URLs
const BASE_URL = "http://localhost:5000";

const urls = {
  home: "/",
  pin: "/pin",
  otp: "/otp",
  securityQuestion: "/security-question",
};

export default BASE_URL;
export { urls };
